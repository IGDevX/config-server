server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: dev

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true

# Set spring.cloud.config.server.git.uri,default-label,clone-on-start